package com.encryptionproject;

public class Validator
{
	public static boolean notANumber(int number)
	{
		boolean isLetter = false;
		String[] validEntries = {"", "Beth", "Adam"};
		
		if (isLetter)
		{
			
		}

		else if (isLetter)
		{
			System.out.println("This is not a valid entry. Please try again");
		}
		return isLetter;
		
	}
}
